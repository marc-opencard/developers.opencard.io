---
title: OAuth client
description: OAuth client entity in the OpenCard EMS data model — machine-to-machine credential with client_id, secret, and scopes for the OpenCard Application API.
---

Machine-to-machine credential (`account_client`) for the Application API. Scoped per integration.

**Key fields:** `client_id`, `secret` (shown once), scopes

→ [Authentication](/getting-started/authentication) · [Overview](/ems/data-model)
