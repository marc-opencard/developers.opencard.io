---
title: Transaction state
description: A transaction state is a point-in-time snapshot of a card transaction — authorized, cleared, deleted, or invoiced — each delivered as its own webhook event.
---

Point-in-time snapshot of a transaction. Each state change fires a separate `card.transaction.*` webhook.

| State | Meaning |
|-------|---------|
| `authorized` | Purchase just happened — preview only |
| `cleared` | Settled — use for accounting |
| `deleted` | Auth reversed |
| `invoiced` | Has invoice number from issuer |

→ [Transaction webhooks](/ems/webhooks/transactions) · [Overview](/ems/data-model)
