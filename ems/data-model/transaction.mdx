---
title: Transaction
description: One card purchase in OpenCard — same id across all states, delivered via webhook not API polling.
---

One purchase. Same OpenCard `id` across all states (`authorized` → `cleared` → `invoiced`, etc.). Delivered via **webhook**, not by polling the API.

**Key fields:** `id`, `reference_id` (from card holder), amounts, merchant, `receiptable`, `card_issuer_reference`

→ [Transaction webhooks](/ems/webhooks/transactions) · [Parallel transaction feeds](/ems/parallel-transaction-feeds) · [Overview](/ems/data-model/index)
