---
title: Transaction
description: A transaction represents one card purchase in OpenCard, keeps the same id across authorized, cleared, and invoiced states, and is delivered via webhook.
---

One purchase. Same OpenCard `id` across all states (`authorized` → `cleared` → `invoiced`, etc.). Delivered via **webhook**, not by polling the API.

**Key fields:** `id`, `reference_id` (from card holder), amounts, merchant, `receiptable`, `card_issuer_reference`

→ [Transaction webhooks](/ems/webhooks/transactions) · [Parallel transaction feeds](/ems/parallel-transaction-feeds) · [Overview](/ems/data-model)
