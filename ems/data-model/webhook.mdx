---
title: Webhook
description: Webhook entity in the OpenCard EMS data model — per-organization HTTP endpoint with URL, secret, verification challenge, and per-event subscription flags.
---

Per-organization HTTP endpoint for events. Must pass the challenge handshake before `active=true`.

**Key fields:** `url`, `secret`, `active`, `enabled`, event boolean flags, `authentication_type`

Optional **groups** filter events by buyer party. **Headers** add custom HTTP headers on delivery.

→ [Webhook setup](/ems/webhooks/setup) · [Events](/ems/webhooks/events) · [Overview](/ems/data-model/index)
