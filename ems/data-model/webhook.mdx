---
title: Webhook
description: A webhook is a per-organization HTTP endpoint for OpenCard events with a URL, signing secret, challenge handshake verification, and per-event boolean flags.
---

Per-organization HTTP endpoint for events. Must pass the challenge handshake before `active=true`.

**Key fields:** `url`, `secret`, `active`, `enabled`, event boolean flags, `authentication_type`

Optional **groups** filter events by buyer party. **Headers** add custom HTTP headers on delivery.

→ [Webhook setup](/ems/webhooks/setup) · [Events](/ems/webhooks/events) · [Overview](/ems/data-model)
